.container
  .row
    .span12
      %table.table.table-hover.table-condensed
        %thead
          %tr
            %th
              Nome do Bar
            %th
              Checkins
            %th
              Usuários
            %th
              Tips
        %tbody
          -@local.each do |l|
            %tr
              %td
                =link_to l.name, :controller => :local, :action => :show, :id => l.id
              %td
                =l.stats.checkinsCount
              %td
                =l.stats.usersCount
              %td
                =l.stats.tipCount
  .row-fluid
    .span12
      #graph
-#
  =javascript_include_tag "linechart"